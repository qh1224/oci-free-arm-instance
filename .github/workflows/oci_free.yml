name: Try to Create OCI VM

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  run-oci-cli:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install and Run OCI Task
        env:
          OCI_USER: ${{ secrets.OCI_CLI_USER }}
          OCI_TENANCY: ${{ secrets.OCI_CLI_TENANCY }}
          OCI_REGION: ${{ secrets.OCI_CLI_REGION }}
          OCI_FINGERPRINT: ${{ secrets.OCI_CLI_FINGERPRINT }}
          OCI_KEY: ${{ secrets.OCI_CLI_KEY_CONTENT }}
          AD_NAME: ${{ secrets.AD_NAME }}
          COMPARTMENT_ID: ${{ secrets.OCI_COMPARTMENT_ID }}
          IMAGE_ID: ${{ secrets.IMAGE_ID }}
          SUBNET_ID: ${{ secrets.OCI_SUBNET_ID }}
          SSH_KEY: ${{ secrets.SSH_PUBLIC_KEY }}
        run: |
          # 1. 安装 OCI CLI
          bash -c "$(curl -L https://raw.githubusercontent.com/oracle/oracle-cli/master/scripts/install/install.sh)" -- --accept-all-defaults || bash -c "$(curl -L https://raw.githubusercontent.com/oracle/oci-cli/master/scripts/install/install.sh)" -- --accept-all-defaults
          export PATH=$PATH:/home/runner/bin
          
          # 2. 写入认证配置
          mkdir -p ~/.oci
          echo "$OCI_KEY" > ~/.oci/oci_api_key.pem
          chmod 600 ~/.oci/oci_api_key.pem
          
          # 彻底清洗可能带入的等号或多余空格
          CLEAN_USER=$(echo "$OCI_USER" | xargs)
          CLEAN_FINGERPRINT=$(echo "$OCI_FINGERPRINT" | xargs)
          CLEAN_TENANCY=$(echo "$OCI_TENANCY" | xargs)
          CLEAN_REGION=$(echo "$OCI_REGION" | tr -d '= ' | xargs)
          
          cat << EOF > ~/.oci/config
          [DEFAULT]
          user=$CLEAN_USER
          fingerprint=$CLEAN_FINGERPRINT
          key_file=~/.oci/oci_api_key.pem
          tenancy=$CLEAN_TENANCY
          region=$CLEAN_REGION
          EOF
          
          chmod 600 ~/.oci/config
          
          # 3. 极致脱水清洗公钥，强行捏成单行
          printf "%s" "$SSH_KEY" | xargs > ~/.oci/ssh_authorized_keys.pub
          
          # 4. 变量严格对齐清洗
          V_AD=$(echo "$AD_NAME" | tr -d '= ' | xargs)
          V_COMP=$(echo "$COMPARTMENT_ID" | tr -d '= ' | xargs)
          V_IMAGE=$(echo "$IMAGE_ID" | tr -d '= ' | xargs)
          V_SUBNET=$(echo "$SUBNET_ID" | tr -d '= ' | xargs)
          
          export OCI_CLI_SUPPRESS_FILE_PERMISSIONS_WARNING=True
          export SUPPRESS_LABEL_WARNING=True
          
          # 终极修复：使用防弹级引号拼接注入，彻底避开甲骨文重复字段的解析 BUG
          /home/runner/bin/oci compute instance launch \
            --availability-domain "$V_AD" \
            --compartment-id "$V_COMP" \
            --shape "VM.Standard.A1.Flex" \
            --shape-config '{"ocpus":2,"memoryInGBs":12}' \
            --subnet-id "$V_SUBNET" \
            --ssh-authorized-keys-file ~/.oci/ssh_authorized_keys.pub \
            --assign-public-ip true \
            --display-name "chunchuan-arm-vm" \
            --source-details '{"sourceType": "image", "imageId": "'"$V_IMAGE"'", "bootVolumeSizeInGBs": 100}' \
            --debug > oci_success.log 2> oci_error.log || true

          # 5. 暴力打印最后 300 行底层抓包日志
          echo "=========================================="
          echo "▶▶▶ 甲骨文 Debug 现场无修剪原始日志 ◀◀◀"
          echo "=========================================="
          if [ -s oci_success.log ]; then
            cat oci_success.log
          else
            echo "--- 错误日志后 300 行截取 ---"
            tail -n 300 oci_error.log
          fi
